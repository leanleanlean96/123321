apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  labels:
    app: postgres
type: Opaque
stringData:
  # ВНИМАНИЕ: в проде эти значения приходят из CI / Sealed Secrets / External Secrets.
  # Здесь — заглушки, которые НЕ должны попадать в реальный prod.
  username: todo
  postgres-password: REPLACE_VIA_CI_OR_SEALED_SECRETS
  database: todo
