apiVersion: v1
kind: Service
metadata:
  name: todo-frontend
  labels:
    app: todo-app
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: todo-app
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
