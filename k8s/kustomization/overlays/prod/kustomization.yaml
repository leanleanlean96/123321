apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: todo-prod

resources:
  - namespace.yaml
  - ../../base
  - secret.yaml

patches:
  - path: patch-resources.yaml

labels:
  - pairs:
      environment: prod
    includeSelectors: false
